[user]
# git config -f ~/.gitconfig.local user.name John Appleseed
# git config -f ~/.gitconfig.local user.email me@example.com

[include]
path = .gitconfig.local

[color]
ui = auto

[core]
excludesfile = ~/.gitignore

[push]
default = matching

[merge]
log = true
conflictstyle = diff3

[rerere]
enabled = 1

[gitflow "branch"]
master = master
develop = develop

[gitflow "prefix"]
feature = feature/
release = release/
hotfix = hotfix/
support = support/
#versiontag =

# [git-achievements]
# achievement.upload = "true"
