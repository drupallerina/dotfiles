

  # packagemanagers
  # known values are: enabled_packagemanagers=("pacman" "npm" "rvm" "fresh" "apm" "aur" "pip" "go" "composer" "gnome_ext")
  enabled_packagemanagers=("composer")
  # Paths
  logfile_path=./logs

    # absolute dir path of this script
    #readonly SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
    list_path=~/.friendlyarch/defaults
    # variable identifier definition
    #readonly VAR_ID='[A-Za-z_]+'

  # fresh repo (enable if you want to use it)
  fresh_repo=drupallerina/dotfiles
